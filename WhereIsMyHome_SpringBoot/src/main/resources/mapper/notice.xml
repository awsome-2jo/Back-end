<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.myHome.model.mapper.NoticeMapper">
	
	<insert id="insertNotice" parameterType="noticeDto" useGeneratedKeys="true" keyProperty="no">
		insert into notice ( title, regDate, hit, content )
		values( #{title}, now(), 0, #{content})
	</insert>
	
	<delete id="deleteNotice" parameterType="int">
		delete from notice
		where no = #{no}
	</delete>
	
	
	<update id="updateNotice" parameterType="noticeDto">
		update notice
		set title = #{title}, content = #{content}, regDate = now()
		where no = #{no}
	</update>
	
	<select id="selectNoticeByNo" parameterType="int" resultMap="noticeDto">
		select no, title, regDate, hit, content
		from notice
		where no = #{no}
	</select>
	
	<update id="countHitByNo" parameterType="int">
		update notice
		set hit = hit + 1
		where no = #{no}
	</update>
	
	<select id="selectNoticeList" resultMap="noticeDto">
		select no, title, regDate, hit
		from notice
		order by reg_date desc, no desc
	</select>
</mapper>